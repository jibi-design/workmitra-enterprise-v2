// src/shared/rating/ratingStorage.ts
//
// Facade — localStorage storage for both rating directions.

export type { EditResult } from "./ratingStorage.constants";
export { ratingStorage } from "./ratingStorage.store";
