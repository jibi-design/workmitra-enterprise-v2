// src/features/employee/home/pages/EmployeeHomePage.tsx
import { useNavigate } from "react-router-dom";
import { ROUTE_PATHS } from "../../../../app/router/routePaths";

export function EmployeeHomePage() {
  const nav = useNavigate();

  return (
    <div>
      <div className="wm-stats">
        <div className="wm-stat">
          <div className="k">Active Shifts</div>
          <div className="v">0</div>
        </div>
        <div className="wm-stat">
          <div className="k">Applications</div>
          <div className="v">0</div>
        </div>
        <div className="wm-stat">
          <div className="k">Earnings</div>
          <div className="v">₹0</div>
        </div>
      </div>

      <div className="wm-cards">
        <div
          className="wm-card wm-variantShift"
          role="button"
          tabIndex={0}
          onClick={() => nav(ROUTE_PATHS.employeeShiftCenter)}
        >
          <div className="left" style={{ flexDirection: "row", alignItems: "center", gap: 12 }}>
            <span className="wm-iconChip wm-iconChipTint" aria-hidden="true">
              🗓️
            </span>
            <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
              <div className="label">Shift Jobs</div>
              <div className="desc">Find & manage temporary shifts.</div>
            </div>
          </div>
          <span className="wm-badge">0</span>
        </div>

        <div
          className="wm-card wm-variantCareer"
          role="button"
          tabIndex={0}
          onClick={() => window.alert("Career Jobs (Employee): next module")}
        >
          <div className="left" style={{ flexDirection: "row", alignItems: "center", gap: 12 }}>
            <span className="wm-iconChip wm-iconChipTint" aria-hidden="true">
              💼
            </span>
            <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
              <div className="label">Career Jobs</div>
              <div className="desc">Apply for permanent / contract jobs.</div>
            </div>
          </div>
          <span className="wm-badge">0</span>
        </div>

        <div
          className="wm-card wm-variantWorkforce"
          role="button"
          tabIndex={0}
          onClick={() => window.alert("Workforce Ops Hub (Employee): next module")}
        >
          <div className="left" style={{ flexDirection: "row", alignItems: "center", gap: 12 }}>
            <span className="wm-iconChip wm-iconChipTint" aria-hidden="true">
              👥
            </span>
            <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
              <div className="label">Workforce</div>
              <div className="desc">Employer groups & current assignments.</div>
            </div>
          </div>
          <span className="wm-badge">0</span>
        </div>
      </div>

      <div className="wm-footer">
        <a href="#" onClick={(e) => e.preventDefault()}>
          Help
        </a>
        <a href="#" onClick={(e) => e.preventDefault()}>
          Support
        </a>
      </div>
    </div>
  );
}