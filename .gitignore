node_modules
dist
dist-ssr
.vite
.DS_Store
Thumbs.db
*.log
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production
# .env.example is intentionally NOT ignored — commit it as a template for new developers
# TLS material for Docker Nginx (keep directory via certs/.gitkeep)
certs/*.pem
certs/*.key
certs/*.crt
src/index.css.__tmp

# Local backups
*.bak
*.bak2
*.bak_*


# Android Play Store signing / FCM (never commit)
android/keystore.properties
android/keystore/*.jks
android/keystore/*.keystore
android/app/google-services.json
*.aab
